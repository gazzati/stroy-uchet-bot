services:
  stroy-uchet-bot:
    image: gazzati/stroy-uchet-bot:${IMAGE_TAG:-latest}
    restart: unless-stopped
    env_file: .env
    command: ["sh", "-c", "node build/db/migrate.js && node build/index.js"]
    extra_hosts:
      - "host.docker.internal:host-gateway"
